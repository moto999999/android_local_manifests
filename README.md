# Lineage lemonadep local repos
========================

``` bash
mkdir -p .repo/local_manifests
curl https://raw.githubusercontent.com/moto999999/local_manifests/lineage-21/lemonadep.xml > .repo/local_manifests/lemonadep.xml
repo sync
```
